# Proposal: Code Cleanup Review — DRY & Logic Improvements
Generated by: cm:cm-tdd (code review)
Date: 2026-04-16

---

## Why we're doing this

Sau khi review toàn bộ codebase (`src/`), phát hiện:
- Logic trùng lặp giữa các file
- Hàm utility định nghĩa nhiều lần
- Logic timezone không nhất quán
- Pre-existing TypeScript errors

---

## What's changing

### Task Breakdown

#### 🔴 HIGH

| ID | Task | File | Fix |
|----|------|------|-----|
| H1 | ~~`selectQuadrant` trùng 3 lần~~ | ~~✅ Done 2026-04-16~~ | Đã fix |
| H2 | `slugify` trùng 2 lần | `import-parser.ts` | Import từ `utils.ts`, xóa bản trùng |
| H3 | `todayStr()` UTC inconsistency | `streak.ts` | Dùng `getTodayBoundary()` 4 AM |

#### 🟡 MEDIUM

| ID | Task | File | Fix |
|----|------|------|-----|
| M1 | `mapRecordToCardProgress()` utility | `challenge-logic.ts` | Gom mapping ở 4 nơi thành 1 hàm |
| M2 | `normalize` trùng logic | `tag-engine.ts` | Tạo `stripDiacritics()` trong `utils.ts` |
| M3 | `ReviewChallenge` type trùng | `useReviewSession.ts` | Re-export từ `challenge-logic.ts` |
| M4 | `upsertSrsRecord` 2 round-trips | `storage/session.ts` | Upsert trực tiếp (1 call) |
| M5 | `fetchDashboardStats` hardcoded | `storage/auth.ts` | Fill thực hoặc bỏ khỏi return type |
| M6 | Comment không khớp logic | `useFlashcard.ts` | Sửa comment +/or logic |

#### 🟢 LOW

| ID | Task | File | Fix |
|----|------|------|-----|
| L1 | `parseSheetsUrlIntoRows` alias thừa | `import-parser.ts` | Xóa alias |
| L2 | Gộp 2 hàm `selectQuadrant*` | `challenge-logic.ts` | Unified 1 hàm |
| L3 | `fetchTopicWordCounts` optimization | `storage/mastery.ts` | JOIN → RPC |

---

## Done ✅

- **H1**: `selectQuadrantForMasteryWord` — Added to `challenge-logic.ts`, `useFreeStudySession.ts` updated (2026-04-16)
  - 7 tests added, 114/114 pass

---

## Effort Estimate

| Tier | Count | Total |
|------|-------|-------|
| HIGH | 2 (H2, H3) | ~20 min |
| MEDIUM | 4 (M1–M4) | ~60 min |
| LOW | 3 (L1–L3) | ~30 min |
| **Total** | **9 tasks** | **~110 min** |
