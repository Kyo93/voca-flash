# Implementation Checklist: Milestone Reward Character Collection
Generated by: cm-planning
Date: 2026-04-16
Phase: Foundation First → Incremental Build

---

## Phase 0: Database Migration (Foundation — no code depends on this)
- [ ] 0.1 Create Supabase migration `026_character_collection.sql`
  - Add `milestones` table (seeded with 8 rows)
  - Add `user_achievements` table
  - Add `user_characters` table
  - Add `user_profiles.total_earned_xp` column
  - Add `user_character_pending` table (for unlocked-before-slot-available)
- [ ] 0.2 Apply migration to Supabase project
- [ ] 0.3 Verify tables exist: `milestones`, `user_achievements`, `user_characters`, `user_character_pending`

---

## Phase 1: Types + Character Metadata (Foundation)
- [ ] 1.1 Add interfaces to `src/lib/types.ts`
  - `CharacterType` union type
  - `UserCharacter` interface
  - `Milestone` interface
  - `MilestoneDefinition` interface
  - `XPWallet` interface
- [ ] 1.2 Create `src/lib/character-types.ts`
  - Export `CHARACTER_METADATA` constant (8 entries with name_vi, name_en, color, bg_tint, animation_class, description)
  - Export `getCharacterMeta(type: CharacterType)` helper
- [ ] 1.3 Create `src/lib/xp-engine.ts`
  - Export `XP_PER_RATING` constant
  - Export `MASTERY_BONUS_XP` constant
  - Export `calculateSessionXp(cards: CardResult[])` function
  - Export `LEVEL_THRESHOLDS` constant
  - Export `getLevelFromInvestedXp(investedXp: number)` function
  - Export `getXpToNextLevel(investedXp: number)` function

---

## Phase 2: Supabase Storage Functions (Foundation)
- [ ] 2.1 Add to `src/lib/supabase-storage.ts`
  - `fetchUserCharacters(userId): Promise<UserCharacter[]>`
  - `upsertUserCharacter(userId, char): Promise<UserCharacter>`
  - `investCharacterXp(userId, charId, amount): Promise<{investedXp, level}>`
  - `fetchUnlockedMilestones(userId): Promise<string[]>` (milestone IDs already achieved)
  - `addSessionXp(userId, amount): Promise<void>`
  - `getAvailableXpBalance(userId): Promise<number>` (total_earned - sum of invested_xp)
  - `getUserPendingCharacters(userId): Promise<UserCharacter[]>` (from user_character_pending)
- [ ] 2.2 Add to `src/lib/admin-queries.ts`
  - `checkAndUnlockMilestone(userId): Promise<MilestoneUnlockResult>`
  - `getNextMilestoneProgress(userId): Promise<{milestoneId, current, target, charType}>`

---

## Phase 3: Milestone Engine (Core Logic)
- [ ] 3.1 Create `src/lib/milestone-engine.ts`
  - Export `MILESTONE_DEFINITIONS` constant (8 milestone configs)
  - Export `evaluateMilestoneCondition(milestone, userStats): boolean`
  - Export `checkAllMilestones(userStats, achievedIds): Milestone[]` (returns newly achievable)
  - Export `getNextMilestoneWithProgress(userStats, achievedIds): MilestoneProgress`
- [ ] 3.2 Write unit tests for milestone-engine in `tests/unit/milestone-engine.test.ts`
  - Test streak milestone evaluation
  - Test mastered count milestone evaluation
  - Test `getNextMilestoneWithProgress` returns correct progress %
  - Test already-achieved milestones not re-returned

---

## Phase 4: useCharacterCollection Hook
- [ ] 4.1 Create `src/hooks/useCharacterCollection.ts`
  - `characters: UserCharacter[]` — all unlocked characters
  - `pendingCharacters: UserCharacter[]` — waiting for slot
  - `walletBalance: number` — available XP to invest
  - `loadCharacters()` — fetch from storage
  - `investXp(charId, amount)` — call storage, update local state
  - `openCharacter(id)` — set active character for detail panel
  - `closeCharacter()` — clear active
- [ ] 4.2 Add `CharacterCollectionContext` wrapper in `App.tsx`

---

## Phase 5: Character Sprites + Animations (Visual Foundation)
- [ ] 5.1 Create `src/components/character/CharacterSprite.tsx`
  - Props: `type: CharacterType`, `size?: 'sm' | 'md' | 'lg'`
  - Render unique SVG for each character type
  - Apply `anim-[type]` class from `character-types.ts` metadata
- [ ] 5.2 Add CSS animations to `src/index.css`
  - `.anim-spider` { animation: spider-wiggle 1s infinite }
  - `.anim-butterfly` { animation: wing-flap 0.5s infinite }
  - `.anim-seedling` { animation: sway 2s infinite }
  - `.anim-tree_boy` { animation: breathe 3s infinite }
  - `.anim-mini_dragon` { animation: float-up-down 1.5s infinite }
  - `.anim-farmer` { animation: idle-bounce 2s infinite }
  - `.anim-bonsai` { animation: sway 3s infinite }
  - `.anim-golden_dragon` { animation: glow-float 1s infinite }
  - `.animate-bounce-in` { animation: scale 0→1 200ms ease-out }
  - `.animate-float-up` { animation: translateY + fade 800ms }
  - `.animate-sparkle` { animation: sparkle 600ms }
- [ ] 5.3 Verify: all 8 character sprites render correctly with idle animations

---

## Phase 6: RightSidebar — Character Shelf
- [ ] 6.1 Create `src/components/character/CharacterSlot.tsx`
  - Props: `character?: UserCharacter`, `locked?: boolean`, `requirement?: string`, `slotNumber: number`
  - Unlocked: character sprite + level badge + level dots + click handler
  - Locked: lock icon + requirement text
- [ ] 6.2 Create `src/components/character/CharacterShelf.tsx`
  - 5 slots rendered in 2-col grid
  - Header: "KHO TÀNG NHÂN VẬT" + "[unlockedCount/5]" pill
  - Fetch characters via `useCharacterCollection`
  - `onCharacterClick(character)` → open detail panel
- [ ] 6.3 Integrate into `src/components/RightSidebar.tsx`
  - Add `<CharacterShelf />` between StreakWidget and Reminders section
  - XP Wallet display below shelf
- [ ] 6.4 Write test: `tests/unit/character-shelf.test.ts`
  - Renders 5 slots
  - Shows unlocked characters with level
  - Shows locked slots with requirement text

---

## Phase 7: Character Detail Panel
- [ ] 7.1 Create `src/components/character/CharacterDetail.tsx`
  - Slide-over panel from right (fixed right-0)
  - Props: `character: UserCharacter`, `onClose()`, `walletBalance: number`
  - Show: character sprite (lg), name, type badge, level progress bar
  - Show: XP to next level, invested stats
  - "Nạp XP" button: open amount input modal (amounts: 50, 100, 200)
  - Level-up animation trigger when level increases
- [ ] 7.2 Write test: `tests/unit/character-detail.test.ts`
  - Renders all character stats
  - "Nạp XP" disabled when balance = 0

---

## Phase 8: XP Investment Flow
- [ ] 8.1 Add `FeedXpModal` component (inside CharacterDetail or standalone)
  - Amount selector: 50 / 100 / 200 XP buttons
  - Confirm button → call `investXp(charId, amount)` from hook
  - Shows new level preview before confirming
- [ ] 8.2 Level-up animation
  - When `new_level > old_level`: trigger sparkle burst CSS animation
  - Character cameo on StudyPage does level-up animation

---

## Phase 9: Milestone Unlock Popup
- [ ] 9.1 Create `src/components/character/UnlockPopup.tsx`
  - Props: `character: UserCharacter`, `onDismiss()`
  - Dark overlay + centered card
  - Sparkle CSS particles + character reveal animation
  - "CHÚC MỪNG!" heading + milestone text
  - "+{xpEarned} XP" floating text
  - "Khám phá thêm →" button
- [ ] 9.2 Integrate into `src/pages/StudyPage.tsx`
  - On session complete: call `checkAndUnlockMilestone()` via `admin-queries`
  - If result.unlocked: show `<UnlockPopup />` with character

---

## Phase 10: StudyPage Cameo + Ribbon Banner
- [ ] 10.1 Create `src/components/character/CharacterCameo.tsx`
  - Props: `character: UserCharacter`, `size?: number`
  - 64x64px fixed position bottom-right
  - Idle animation (CSS bounce loop)
  - `+{xp} XP` float text on `onXPEarned` prop
  - Expression changes (normal / happy / concerned) based on `mood` prop
- [ ] 10.2 Create `src/components/character/MilestoneRibbon.tsx`
  - Props: `nextMilestone: MilestoneProgress | null`
  - Sticky top banner showing next locked character requirement + progress bar
  - Click → open CharacterDetail for that locked character
- [ ] 10.3 Integrate into `src/pages/StudyPage.tsx`
  - Import `CharacterCameo` (bottom-right corner, outside flashcard area)
  - Import `MilestoneRibbon` (sticky top)
  - Pass active character from `useCharacterCollection` to cameo
  - Trigger cameo animation on card rating (correct/wrong/level-up)

---

## Phase 11: Dashboard Integration
- [ ] 11.1 Create `src/pages/CharacterCollectionPage.tsx`
  - Full collection view: all 5 slots + pending characters
  - Character detail on click
  - Route: `/characters`
- [ ] 11.2 Add route to `src/App.tsx`
- [ ] 11.3 Add collection summary card to `src/pages/DashboardPage.tsx`
  - "Tổng số nhân vật: X/8" + progress ring + "Khám phá" button

---

## Phase 12: Milestone Trigger in useFlashcard
- [ ] 12.1 Modify `src/hooks/useFlashcard.ts`
  - After session completes (`handleSessionComplete`): call `checkAndUnlockMilestone(user.id)`
  - Dispatch XP to wallet via `addSessionXp`
  - If milestone unlocked: show `UnlockPopup` state
- [ ] 12.2 Write integration test: `tests/unit/study-session-xp.test.ts`
  - Complete session with 10 cards
  - Verify `total_earned_xp` updated
  - Verify milestone check called

---

## Phase 13: Polish + Edge Cases
- [ ] 13.1 Empty state: user with 0 characters → all 5 slots locked, hint "Hoàn thành mốc 7 ngày để mở khóa slot đầu tiên"
- [ ] 13.2 Slot unlock monthly: add `monthsActive` calculation, test that 6th slot appears after 6 months
- [ ] 13.3 Character level-up: verify level computed correctly for all threshold values (0, 200, 500, 1000, 2000)
- [ ] 13.4 XP overflow: invest more XP than wallet → button disabled, show "Không đủ XP"
- [ ] 13.5 Pending inventory: if user unlocks character before slot opens → character waits, appears on shelf when slot opens

---

## Testing Checklist (before merge)

| Test | File | Check |
|---|---|---|
| Character types exported | `types.test.ts` | All 8 types valid |
| Level derivation | `xp-engine.test.ts` | All 5 thresholds correct |
| Milestone conditions | `milestone-engine.test.ts` | All 8 milestone evaluations |
| Shelf renders 5 slots | `character-shelf.test.ts` | 5 div elements |
| Locked slot shows requirement | `character-shelf.test.ts` | requirement text visible |
| XP invest deducts wallet | `xp-engine.test.ts` | balance decreases |
| Session XP calculation | `xp-engine.test.ts` | correct totals |
| All sprites render | `character-sprite.test.ts` | 8 SVG elements |