# Proposal: Code Cleanup & Refactor — Logic, Naming & Performance
Generated by: cm-brainstorm-idea
Date: 2026-04-15

## Tại sao chúng ta làm điều này

### Qualified Problem
**Cho:** Developer đang maintain và phát triển VocaFlash
**Ai:** Cần hiểu rõ luồng logic, tránh duplicate code, đặt tên chính xác
**VocaFlash** là một vocabulary learning app với kiến trúc đã ổn định nhưng tồn tại **logic trùng lặp, naming không nhất quán, và một số data flow thiếu hiệu quả**
**Khác với:** Việc refactor thông thường — đây là cleanup tập trung vào **correctness, maintainability, và performance**

### 9 Windows Analysis

```
┌─────────────────┬───────────────────────────┬───────────────────────────┬───────────────────────────┐
│                 │         PAST              │        PRESENT            │         FUTURE            │
├─────────────────┼───────────────────────────┼───────────────────────────┼───────────────────────────┤
│ SUPER-SYSTEM    │ React ecosystem best      │ Codebase có ~50 src files │ Type-safe, tree-shakable│
│ (ecosystem)     │ practices đã định hình    │ đã phân layer tốt        │ codebase sẵn sàng scale  │
├─────────────────┼───────────────────────────┼───────────────────────────┼───────────────────────────┤
│ SYSTEM          │ localStorage + SM-2       │ Supabase + FSRS + Review  │ Full-Featured SaaS       │
│ (VocaFlash)     │ độc lập                  │ Arena đầy đủ             │ với multi-tenant + auth  │
├─────────────────┼───────────────────────────┼───────────────────────────┼───────────────────────────┤
│ SUB-SYSTEM      │ Storage rời rạc,          │ Đã centralize nhưng       │ Storage unified,         │
│ (Components)    │ logic trùng lặp           │ còn duplicate nhỏ        │ hooks tái sử dụng cao    │
└─────────────────┴───────────────────────────┴───────────────────────────┴───────────────────────────┘
```

### Root Cause(s):
1. **Tên hàm không nhất quán**: `fetchReviewWords` nhưng `fetchMasteryStats` — không có convention thống nhất (verb + resource)
2. **Logic trùng lặp nhỏ**: `selectQuadrant` bị duplicate giữa `useReviewSession` và `useFreeStudySession`
3. **Cache layer thiếu**: Streak tính toán trên mỗi render mà không cache
4. **Data flow rải rác**: Streak data chạy song song localStorage + Supabase mà không có unified cache
5. **StudyPage tái tạo layout**: Trong khi đã có `AppLayout` — vi phạm DRY
6. **Dead code nhẹ**: `RoadmapContext` không được consume ở bất kỳ đâu ngoài definition

### Impact if NOT addressed:
- **Developer experience**: Khó đọc code, dễ nhầm lẫn khi thêm feature mới
- **Performance**: Fetch streak 2 lần (localStorage + Supabase), không có memoization
- **Maintainability**: Duplicate `selectQuadrant` → khi đổi logic phải sửa 2 chỗ
- **Bug risk**: Streak display chỉ dùng `loadStreak()` (localStorage) trong khi user đã login

---

## What's Changing

### Issues Found (Priority Breakdown)

#### 🔴 CRITICAL — Logic / Flow

| # | Issue | Location | Impact |
|---|-------|----------|--------|
| C1 | `StudyPage` tái tạo grid layout thay vì reuse `AppLayout` | `StudyPage.tsx` | Layout inconsistency, code bloat |
| C2 | Streak display chỉ dùng localStorage — bỏ qua Supabase streak đã tính | `Sidebar.tsx:11` | Streak không chính xác với logged-in user |
| C3 | `useReviewSession.selectQuadrant` và `useFreeStudySession.selectQuadrant` — CÙNG logic, KHÁC file | `useReviewSession.ts`, `useFreeStudySession.ts` | DRY violation, bug khi logic đổi |
| C4 | Streak fetch chạy trên mỗi render (`RightSidebar`) — không cache | `RightSidebar.tsx:14-24` | Performance: redundant Supabase calls |

#### 🟡 MEDIUM — Naming / Convention

| # | Issue | Location | Impact |
|---|-------|----------|--------|
| M1 | `fetchMasteryStats` vs `fetchUserVocabulary` — inconsistent prefix | `storage/mastery.ts`, `storage/session.ts` | Confusing when scanning |
| M2 | `getRoadmapStats` trong `roadmap.ts` không dùng RPC, chỉ dùng khi không có userId | `storage/roadmap.ts:97-115` | Dead logic path |
| M3 | `mapWordToCard` là internal helper — nên là private hoặc extract | `storage/mastery.ts:79-90` | Leaked abstraction |
| M4 | `useAdminTopics` export `slugify` — đặt ở sai layer (logic không phải hook) | `hooks/admin/useAdminTopics.ts:11-22` | Violates "hooks = side effects only" |

#### 🟢 LOW — Dead Code / Cleanup

| # | Issue | Location | Impact |
|---|-------|----------|--------|
| L1 | `RoadmapContext` — chưa được consume ở component nào | `contexts/RoadmapContext.tsx` | Dead code, có thể remove |
| L2 | `useFreeStudySession` — import `ReviewChallenge` từ `useReviewSession` | `useFreeStudySession.ts:6` | Cross-import circular tiềm tàng |
| L3 | `Sidebar.tsx` gọi `getStreakDisplay()` — sync call với logged-in user | `Sidebar.tsx:11` | Ignores Supabase streak, shows stale data |
| L4 | `import type { Card } from '../lib/srs'` trong `StudyPage` — nhưng Card được dùng với fields khác | `StudyPage.tsx:9` | Type mismatch tiềm tàng |

---

## Options Evaluated

### Option A: Targeted Hot-Fix (Quick Wins)
**Approach:** Chỉ fix những issue có thể gây bug ngay lập tức.
**Effort:** S (1-2 giờ)

**What changes:**
- Fix C2 + C3 (Streak display, selectQuadrant extraction)
- KHÔNG đụng đến StudyPage layout refactor
- KHÔNG đụng đến dead contexts

**Pros:** An toàn, không regression, nhanh
**Cons:** Không giải quyết root cause, còn lại issues để sau

---

### Option B: Comprehensive Cleanup (Full Fix) ⭐
**Approach:** Fix tất cả issues theo priority, extract shared logic, fix naming.
**Effort:** M (4-6 giờ)

**What changes:**
- **C1**: StudyPage reuse AppLayout → Remove duplicate grid
- **C2**: Sidebar + RightSidebar use `fetchStreakFromSupabase` (already exists)
- **C3**: Extract `selectQuadrant` → `src/lib/challenge-logic.ts`, import vào cả 2 hooks
- **C4**: RightSidebar cache streak với React state hoặc `useRef`
- **M1-M4**: Rename functions, move `slugify`, remove dead logic
- **L1**: Remove `RoadmapContext` (dead code)
- **L2**: Move shared types sang `src/lib/types.ts`

**Pros:** Rõ ràng, maintainable, giải quyết root cause
**Cons:** Nhiều file thay đổi, cần test kỹ

---

### Option C: Conservative Naming Pass Only
**Approach:** Chỉ rename functions cho nhất quán, không đụng đến logic.
**Effort:** XS (1 giờ)

**What changes:**
- Rename: `fetchMasteryStats` → `getMasteryStats`, v.v.
- Remove `RoadmapContext` dead code
- Move `slugify` ra khỏi hook

**Pros:** Nhanh nhất, an toàn
**Cons:** Không fix được logic issues, performance vẫn kém

---

## 🎯 Recommendation

**Chọn Option B: Comprehensive Cleanup (Full Fix)**

### Tại sao Option B:
1. **Fix root cause — không chỉ symptom**: Streak display sai là vì Sidebar gọi `loadStreak()` thay vì `fetchStreakFromSupabase`. Chỉ rename không giải quyết được.
2. **DRY violation đã rõ ràng**: `selectQuadrant` trùng lặp → khi thêm challenge type mới phải sửa 2 chỗ → bug chắc chắn.
3. **Performance**: RightSidebar fetch streak mỗi render → ảnh hưởng UX.
4. **Tương lai**: StudyPage đang tái tạo grid → khi thêm feature mới vào layout phải sửa 2 chỗ.

### Tại sao KHÔNG các options khác:
- **Option A**: Chỉ hot-fix → sẽ phải quay lại fix tiếp, lãng phí effort.
- **Option C**: Chỉ rename → không giải quyết bug streak, performance vẫn kém, DRY vẫn vi phạm.

### Quick-win suggestion:
Có thể bắt đầu với **fix C2 (Streak display)** — chỉ 1 dòng thay đổi trong `Sidebar.tsx` để thấy kết quả ngay:

```tsx
// Thay:
const streak = getStreakDisplay()
// Bằng:
const { user } = useAuth()
const [streak, setStreak] = useState(loadStreak())
useEffect(() => {
  if (user) fetchStreakFromSupabase(user.id).then(setStreak)
  else setStreak(loadStreak())
}, [user])
```

---

## Next Steps for cm-planning

1. **Extract shared logic**: `selectQuadrant` → `src/lib/challenge-logic.ts`
2. **Fix StudyPage**: Replace duplicate grid → Use `AppLayout` pattern (có thể tạo immersive variant)
3. **Fix Sidebar streak**: Dùng `fetchStreakFromSupabase` thay vì `loadStreak()`
4. **Cache streak**: Thêm memoize/caching cho streak fetch
5. **Rename pass**: Consistency theo convention `get/fetch` prefix
6. **Remove dead code**: `RoadmapContext`, unused `getRoadmapStats` path
7. **Move utilities**: `slugify` → `src/lib/utils.ts`, `mapWordToCard` → private or extract

## Context for downstream skills

- **cm-planning**: Tập trung vào 7 tasks rõ ràng, không ambiguity
- **cm-tdd**: Test streak display (localStorage vs Supabase), test `selectQuadrant` logic
- **cm-ux-master**: Không có thay đổi UI — chỉ code refactor
- **cm-execution**: Suggested mode: single-session, 7 files changes

---

## Technical Debt Score Before/After

| Dimension | Before | After |
|-----------|--------|-------|
| DRY violations | 1 critical | 0 |
| Performance issues | 2 (streak fetch) | 0 |
| Naming inconsistency | 4 | 0 |
| Dead code files | 1 (RoadmapContext) | 0 |
| Layout duplication | 1 (StudyPage) | 0 |

